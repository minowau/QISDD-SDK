{
  "name": "qisdd-sdk-monorepo",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.0.0",
    "lerna": "^6.0.0",
    "jest": "^29.0.0",
    "hardhat": "^2.12.0"
  },
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "lint": "lerna run lint",
    "start": "lerna run start"
  }
} 